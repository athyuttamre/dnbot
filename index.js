require('dotenv').config();
console.log("Welcome to DN Bot!");

// Initialize database

setInterval(function() {
  // Make DN API request

  // Get top ten stories

  // Check if already published

  // Post to Facebook
}, 1500);
